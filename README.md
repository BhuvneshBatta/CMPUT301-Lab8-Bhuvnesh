# CMPUT 301: Lab 8 - CustomList

## Student Details
- **Full Name:** Bhuvnesh Batta
- **CCID:** bbatta

---

## Overview
This project implements and tests the `CustomList` class with three key methods:
- `hasCity()`
- `deleteCity()`
- `countCities()`

Each method was developed using Test-Driven Development (TDD):
1. Write a failing test.
2. Implement the method to make it pass.

---

## Run Tests
To run all tests, use Android Studio’s built-in test runner  
or execute the following command in the terminal:
